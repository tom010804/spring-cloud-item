server:
  port: 8083
spring:
  application:
    name: zuul-item
  redis:
    host: localhost
    port: 6379
    session:
      store-type: redis
eureka:
  client:
    serviceUrl:
      #·þÎñ×¢²áµØÖ·
      defaultZone: http://localhost:8080/eureka/
zuul:
  routes:
    api©\a:
      path: /api©\a/**
      serviceId: feign-item

